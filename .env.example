# 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# ⚠️ .env 文件不要提交到 Git！

# GitHub 用户名（用于站点链接）
GITHUB_USERNAME=your-username

# 工作目录（可选，默认使用相对路径）
# WEIQI_WORKSPACE=/path/to/workspace

# 技能包目录（可选，默认使用相对路径）
# WEIQI_SKILLS_DIR=/path/to/skills
